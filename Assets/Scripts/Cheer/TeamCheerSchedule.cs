/// <summary>
/// 예전 전역 Idle 상수. M/T 함정은 인스펙터 randomIntervalMin/Max를 쓴다.
/// 이 클래스는 더 이상 호출되지 않는다.
/// </summary>
public static class TeamCheerSchedule
{
    public const float IdleMinSeconds = 55f;
    public const float IdleMaxSeconds = 80f;
}
