# T.Stage5 러너 재설계 계획서

**상태(2026-09-21):** **3차 보정 — 좌표계 이동 + 2층 y=50 + 시작 홀을 격자 안으로 + 하향 차단 방식 교체.**
설계 확정 ✅ · 생성기 ✅(seed 7036) · **씬 ✅**(문 **176** + 패드 54 + 외곽 벽 + 시작 방, NavMesh 베이크) ·
**코드 ✅**(§4.2 전건) · **배선 ✅**(§5) · 미로 7장(벽 448개)·맵 7장 **삭제 완료**
**남은 것: V단계 실측(§6) + 2층 함정 배치(§1.9) + Tip 문구.** (지도 패널 배선은 2026-09-21 확인 완료 — §5-12)
**작업 순서:** D(✅) → G(✅) → S(씬 ✅ — §2) → C(코드 ✅ — §4) → 배선(✅ — §5)
→ **V(빌드 2개, §6) ← 지금 여기** → Tip 문구(§7)

> **⚠️ 3차 보정에서 바뀐 것 (2026-09-21) — 구 수치를 그대로 쓰면 전부 어긋난다.**
> ① **좌표계가 0~120 → −12~108로 이동**했다. 칸 중심이 6·18…114에서 **−6·6·18…102**가 됐고
>    start는 **원점**, goal은 **(102,102)** 다(§1.4 좌표계).
> ② **2층이 y=25 → y=50**이 됐다. 1층이 너무 가까워 격자가 안 보였다. 2층 바닥 윗면은 **50.25**다.
> ③ **시작 홀이 격자 밖 별채에서 격자 안 2×2 칸으로** 들어왔다. 그 안쪽 문 4개를 지워
>    **22×22 방 하나**로 합쳤다 — 그래서 문이 180이 아니라 **176**이다(§1.4).
> ④ **2층 하향 차단이 넉백에서 "레인별 바닥 판"으로 바뀌었다**(§1.3 전면 교체).
**이 문서가 T5 SSOT.** `CoopStageAudit.T.md`의 "T5 보류·Runner/Chaser 재설계 금지"는 이 결정으로 해제 예정.
**연관 SSOT:** 색 매핑·커튼 준비 게이트는 `SceneReadyAndColorMapping.md` ·
**부활/사망은 `ReviveSystemDesign.md`**(T5는 부활 예외 — 같은 문서 §7.1) ·
판 랜덤화의 형제 문서는 `TStage4TrapRandomization.md`

---

## 0. 한 줄 요약

10×10 격자의 **칸과 칸 사이 모든 경계가 색 문**이고 **시작은 전부 닫힘**이다.
1층의 **러너 1명**은 혼자서는 한 칸도 못 움직이고, **2층에서 내려다보는 안내자들이 색 패드를 밟아
문을 열어줘야만** 앞으로 간다. 한 번에 한 색만 열리므로 start→goal은 **색을 최소 12번 갈아타는 여정**이다.
체이서는 **문을 무시하고 직선으로** 쫓아오므로, 위험한 순간은 막다른 길이 아니라 **문 앞에서 기다리는 시간**이다.

---

## 1. 확정 결정

### 1.1 구조 — 한 판. 맵도 한 장 (2026-09-20)

> **T5는 T.Stage4와 같은 경로만 탄다.** 씬 로드 → 시드로 러너 1명 + 색 순열 → 시작 → 클리어/실패.
> 스테이지 안에서 상태가 넘어가는 구간(라운드 전환)이 **없다.**

**저장소에서 허용하는 스테이지 형태는 둘뿐이다 (2026-09-19 사용자 확정):**

| 형태 | 예 |
|---|---|
| **(a) 씬 = 스테이지 1개** | T.Stage4, T.Stage5 |
| **(b) `PhaseManager` + phase마다 StageManager** | T.Boss 류 |

| 항목 | 결정 |
|---|---|
| 판 수 | **1** |
| 맵 | **1장.** 7장 추첨 폐기 — 지형이 고정돼도 §1.12의 두 축이 매판 판을 바꾼다 |
| 러너 | **1명.** 솔로 = 본인 |
| 러너 교대 | **없다.** 실패 시 로테이션으로 자연 교대(§1.10) |
| 리로드 시 | 새 시드로 **러너 재추첨 + 고유색 순열 재추첨** |
| 제한 시간 | **110초** (실측 밴드 **100~120**, §1.8) |
| 실패 | **러너 사망 · 안내자 사망 · 시간 초과** |
| 클리어 | **러너가 1층 Goal + 안내자 전원이 2층 Goal** (§1.8) |
| 부활 | **없다 — 누가 죽든 즉시 실패**(§1.8). SSOT는 `ReviveSystemDesign.md` §7.1 |

### 1.2 시작 시나리오

**텔레포트를 쓰지 않는다.** 플레이어는 스폰한 자리에서 물리적으로 2층에 올라간다.

| # | 시점 | 일어나는 일 | 주체 |
|---|---|---|---|
| 0 | 씬 로드 | 색별 고정 스폰 `(0,0,5)/(5,0,0)/(0,0,-5)/(-5,0,0)`. 커튼 덮인 채 | `PlayerSpawnManager` |
| 1 | `OnPlayersReady` | 시드로 **러너 1명 + 고유색 순열** 확정 → 색 매핑 적용 → **러너 발판만 끄기** → 게이트 `t5.map` 해제 → 커튼 걷힘 | 디렉터 |
| 2 | 즉시 | 각자 자기 `ColoredStartZone` 안 → **3초 카운트다운** | `StageStartGate` |
| 3 | 카운트다운 완료 | `StartStage()` + **발판 활성화**(러너 것 제외) → 안내자가 발사통을 타고 2층으로 발사 | `OnCountdownComplete` |
| 3.5 | **안내자가 각자 y≥51 통과** | **그 사람 몫의 바닥 판을 닫는다**(§1.3) — 아직 상승 중일 때 발밑에서 | 디렉터 |
| 4 | **안내자 전원 도착**(또는 안전망 8초) | **남은 판 닫기 → 발판 끄기 → 대각 barrier 해제**. **이 순서가 사양이다** — barrier가 먼저 풀리면 러너가 구멍 뚫린 2층을 등지고 출발한다 | 디렉터 |
| 5 | 러너가 시작 존 이탈 | **체이서 4마리 스폰** | 이탈 트리거 |
| 6 | — | 러너 1층 Goal + 안내자 2층 Goal → 클리어 / 110초 → 실패 | `T5RunnerObjective` |

**왜 이 순서인가 — 세 가지가 이 순서에 걸려 있다:**

1. **색 확정이 `OnPlayersReady`인 이유.** 사망 리로드 때 새 시드가 Host→ClientRpc로 오는데
   `Start()`보다 먼저 도착한다는 보장이 없다. **문 색이 곧 이번 판의 지형**이므로, 커튼이 걷히기 전에
   매핑이 끝나 있어야 한다. 커튼이 덮고 있으므로 플레이어에게는 **"눈 떠보니 이 배색"** 으로 보인다.
   → 게이트 문자열 **`"t5.map"`은 그대로 쓴다**(맵 선택이 사라져도 "이번 판 확정 완료"라는 뜻은 같다).
2. **barrier를 4단계까지 유지하는 이유.** 카운트다운 직후에 내리면 러너가 **옆 칸 발판을 밟아**
   2층으로 올라갈 수 있다. `ContactKnockback`에는 대상을 고르는 기능이 없어서, 발판이 켜져 있는
   동안 barrier로 막는 것이 유일한 구조적 차단이다.
3. **2층 트리거를 3단계에 미리 켜두지 않는 이유.** `ContactKnockback`은 **속도 방향을 보지 않는다.**
   미리 켜면 발사돼 올라가던 사람이 통과하며 **한 번 더 가속**돼 난간(6m)을 넘는다.

> **러너가 시작 존에서 안 나가면 체이서가 안 나온다 — 그대로 둔다.**
> 110초 타임아웃이 실패로 끝내므로 교착이 아니다.

**수직 발사 수치:** `ContactKnockback.VerticalUp`. Player mass = 1이라 force가 곧 초기 속도이고
**정점 높이 ≈ force² / 19.62**.

> **⚠️ 2층이 50으로 올라가면서 force도 다시 잡았다 (2026-09-21).** 구 값 24는 2층이 25일 때의 값이라
> 정점 29로는 **이제 근처에도 못 간다.** 발판 윗면이 y=1.0이므로 **force 33 → 정점 56.5**,
> 2층 바닥 50.25 대비 여유 **6.25m**, 도착 판정선 51을 **2.3초**에 통과한다.
> 난간 상단이 56.25라 정점이 그와 거의 같은데, 발사는 발사통(±11) **안쪽**에서 일어나고
> 난간은 2층 **바깥 가장자리**에 있어 부딪힐 일이 없다. 실측으로 조일 것.

모자라면 시작 홀에 도로 떨어지고 **발판을 다시 밟아 재발사**되므로(`knockbackInterval` 0.25초)
실패가 자기 복구된다.

### 1.3 2층에서 1층으로 못 내려간다 — **레인별 바닥 판** (2026-09-21 전면 교체)

2층 바닥에 **발사 구멍이 하나** 있다(원점 ±11, 22×22 = 시작 방의 천장 전체).
그 구멍은 곧 내려가는 길이기도 하다.

**구멍을 네 조각으로 나눠, 그 조각을 쓸 사람이 지나갈 때만 열어 둔다.**

| 항목 | 결정 |
|---|---|
| 판 | **4장**(`Lid_Blue/Purple/Green/Yellow`). 45° 회전한 **마름모**, scale **15.5564 = 11√2** |
| 배치 | 구멍 네 변의 중점 `(0,11)` `(11,0)` `(−11,0)` `(0,−11)`, y=50 |
| 왜 마름모인가 | 변 중점에 놓인 반대각선 11짜리 마름모 4장은 **22×22를 빈틈없이 정확히 타일링**한다. 그리고 각 조각이 **대각 칸막이로 갈린 삼각형 레인과 정확히 일치**한다 — 즉 한 장 = 한 사람 몫 |
| 상태 규약 | **켜짐 = 닫힘(바닥)**, 꺼짐 = 열림(구멍). 활성 상태와 의미가 반대다 |
| 초기 상태 | **전부 닫힘** |
| 여는 것 | `PickRunnerAndColors()`에서 **이번 판에 실제로 발사될 안내자 색만** |
| 닫는 것 | 그 사람이 **2층 도착 존(y ≥ 51)** 에 들어온 순간 — 아직 **상승 중**일 때 발밑에서 닫힌다 |
| 러너 색 · 없는 색 | **끝까지 닫힌 채.** 올라올 사람이 없으므로 여는 것 자체가 버그다 |
| 솔로 | 안내자 0명 → 한 장도 안 열림 → 2층이 처음부터 완전한 바닥 |

**왜 "전원 도착 시 뚜껑 한 장"이 아닌가 — 교착이 생긴다.**
한 명이 발사에 실패하면, 이미 정점(56.5)까지 올라간 나머지가 **아직 안 닫힌 구멍으로 도로 떨어져**
재발사되며 무한히 튀어오른다. 그 와중에 뚜껑이 닫히면 비행 중이던 사람이 밑에 갇히고,
갇힘을 피하려 "비행 중인 사람이 없을 때만 닫는다"는 가드를 걸면 **영영 안 닫힌다.**
레인별이면 성공한 사람은 **자기가 올라간 그 비행에서** 자기 판이 닫혀 그 위에 착지하고,
실패한 사람만 혼자 남는다.

**왜 판정선이 2층 바닥(50.25)이 아니라 51인가.** 판은 그 사람이 **아직 올라가는 중일 때**
발밑에서 닫혀야 한다. 착지한 뒤에 닫으면 이미 구멍으로 떨어진 뒤다. 정점이 56.5이므로
상승 중 51을 반드시 통과하고, 50.25가 아니라 51인 덕분에 **판이 발 안쪽에서 생성되는 일도 없다.**

<details>
<summary>폐기 — 구멍에 `ContactKnockback` 트리거를 깔아 되밀어 올리는 안 (2026-09-21 폐기)</summary>

`VerticalUp`을 약하게 깐 트리거로 "내려가려 하면 다시 올라감"을 만들려 했다. 코드 0이 장점이었다.
**폐기 이유:** 되밀리는 연출이 거칠고, `ContactKnockback`이 **속도 방향을 안 봐서** 켜는 시점을
미루는 우회(§1.2-3)가 계속 필요했다. 바닥을 만들어 막으면 그 제약이 통째로 사라진다
(2026-09-21 사용자 판단). 같이 사라진 것: `DescentBlocker` 오브젝트 · 디렉터의 `descentBlocker` 필드 ·
"2층 도착 존이 발사통 XZ를 비켜 가야 한다"는 §2의 제약(이중 가속이 없어졌으므로 2층 전체를 덮는다).
</details>

### 1.4 문 — **격자의 모든 변이 문이다** (2026-09-20 전면 교체)

> **구 설계는 "벽으로 만든 미로 위에 문 15개"였다. 그게 쉬웠던 이유는 단순하다 —
> 러너가 문 앞까지는 혼자 갈 수 있었기 때문이다.** 경로 위에 문을 늘려도, 벽 몇 개를 문으로
> 바꿔도 그 성질은 안 변한다. 그래서 **벽을 전부 없애고 전부 문으로 바꾼다.**

| 항목 | 결정 |
|---|---|
| 격자 | **10×10칸** |
| 통로 폭 | **10** (구 8에서 확대 — 한 번 열렸을 때 달리는 거리를 늘리려고) |
| 문 두께 | **2** |
| 피치 | **12** = 통로 10 + 문 두께 2 → 전체 **120×120** |
| 문 개수 | **176개** = 180 − 4. 2 × 10 × 9 = 180에서 **시작 방 내부 경계 4개**(`DoorV_0_0` `DoorV_0_1` `DoorH_0_0` `DoorH_1_0`)를 지웠다 — 네 칸이 한 방이 돼야 `StageStartGate`가 들어간다(2026-09-21) |
| 문 규격 | 폭 10 × 높이 15 × 두께 2 · `openMode = SlideUp` · `latchOnOpen = false` · `requiredPads` 비움 |
| 외곽 | **진짜 벽**(문 아님), **네 면이 전부 막힌다.** 구 설계의 "시작칸 남·서 외벽을 뚫는다"는 시작 홀이 격자 **밖**에 있을 때 이야기였고, 지금은 **안으로 들어왔으므로 뚫을 이유가 없다**(2026-09-21) |
| 색 | **고유 4색(Blue/Purple/Green/Yellow) + 흑 + 백 = 6** — 색당 평균 30개 |
| 시작 상태 | **전부 닫힘** |
| 규칙 | 패드 1개를 밟으면 **그 색 문만 Open, 나머지 전부 Close**(배타). 흑·백도 같은 규칙의 한 색 |
| **점유 = 마지막으로 밟은 패드** | **서 있는 것으로는 점유를 되찾지 못한다.** 발동은 `OnTriggerEnter` 하나뿐 |
| 색 매핑 | **열 수 없는 색은 전부 벽** — 이번 판에 없는 색도, **러너 색도**(2026-09-20 2차 확정). §1.4-1 |
| NavMeshObstacle | **붙이지 않는다** — 체이서가 문을 관통하므로(§1.7) |

**왜 120×120인가 (2026-09-20 사용자 확정, A/B 비교 후 B 채택):**

| | A안: 통로 10 · 문 두께 1 | **B안(채택): 통로 10 · 문 두께 2** |
|---|---|---|
| 피치 | 11 → 전체 110×110 | **12 → 전체 120×120** |
| 칸 중심 | 5.5, 16.5 … **비정수** | **6, 18, … 114 정수** |
| 2층·난간·발사통 | 110으로 **전부 재제작** | **이미 만들어 둔 120 그대로 재사용** |

1. **"몇 걸음 못 가고 선다"를 결정하는 건 통로 폭이 아니라 피치**다. 문 두께 2는 통로 길이에
   그대로 보태진다 — 폭만 넓히고 두께를 깎으면 넓힌 만큼 도로 까먹는다.
2. 120이 유지되면 `T5_Common`(Floor2F·Rails2F·LaunchShaft·DescentBlocker)과 시작 홀이 **그대로 산다.**
   버리는 것은 미로 벽 448개뿐이다.
3. 칸 중심이 정수라 덤프·검증 스크립트가 그대로 돌아간다.

**좌표계**

| 것 | 값 |
|---|---|
| 범위 | **−12 ~ 108** (x, z). 칸 피치 **12** |
| 격자선 (문이 서는 줄) | −12, 0, 12, … , 108 |
| 칸 중심 | −6, 6, 18, … , 102 |
| **시작 방** | 칸 (0,0)(1,0)(0,1)(1,1) **네 칸을 합친 22×22** — 내부 **x,z ∈ [−11, 11]**, 중심이 **원점** |
| goal 칸 중심 | **(102, 102)** — 우상단 |
| 문 자리 | **(격자선, 칸 중심)** 또는 **(칸 중심, 격자선)** — 예: `(12, 6)` |
| 체이서 스폰 4점 | S(102, −8) · W(−8, 102) · N(102, 102) · E(50, 50) — 추격이 시작되면 어차피 흩어지므로 정확한 칸 중심을 요구하지 않는다(2026-09-21 사용자 확정) |
| 2층 바닥 윗면 | **50.25** (`Floor2F` y=50, 두께 0.5). 바닥 범위 −11 ~ 108, 난간은 그 위 6m |
| 발사통(`LaunchShaft`) | 시작 방과 같은 ±11, **y 15 ~ 50.25**. 0~15는 방 자체의 문·외벽이 막으므로 **러너가 문으로 나갈 수 있다** |

> **⚠️ 전환 1회에 움직이는 문은 60개다 — 180개가 아니다.**
> 배타 게이트라 **열려 있던 색 30개가 닫히고 새 색 30개가 열린다.** 나머지 120개는 이미 닫혀 있어
> `Close()`가 무동작이다. 연출·물리 부하를 여기 기준으로 볼 것.

> **⚠️ 문턱에 서 있으면 러너가 튕긴다.** `DoorController`의 닫힘 넉백은 **`Player`에만** 걸린다
> (`TryKnockback`이 `Player` 컴포넌트를 요구 — 체이서는 영향 없음). 전환 순간 러너가 닫히는 문
> 안에 있으면 밀려난다. **이건 버그가 아니라 "문턱에서 어물거리지 마라"는 사양으로 둔다.**

### 1.4-1 ⚠️ **T5에 Common은 없다** — 열 수 없는 색은 전부 벽 (2026-09-20 2차 확정)

**1차 결론은 "러너 색은 Common(열림), 없는 색은 벽"이었다.** 그 절반을 더 밀어 **러너 색도 벽**으로 통일한다.
러너는 1층이라 자기 색 패드를 밟을 수 없으므로 **그 색은 이번 판에 없는 색과 다를 게 없고**,
Common으로 두면 러너 색 문 약 30개가 통째로 "아무나 여는 문"이 되어 격자가 헐거워진다.

| | 벽이 되는 색 | 구간에 보이는 패드 |
|---|---|---|
| 4인 | 러너 1색 | 고유 3 + 흑 + 백 = **5** |
| 3인 | 러너 + 없는 색 1 = 2색 | **4** |
| 2인 | 3색 | **3** |
| 솔로 | 4색 전부 | **0**(§1.6) |

**그래서 이 스테이지에는 Common 문도 Common 패드도 없다.** 부수 효과로
`SceneReadyAndColorMapping.md` §B.5의 "Common 전용 무채색 머티리얼" 숙제가 **T5에서는 사라진다**
(T1/T4에는 그대로 남는다 — 그쪽의 "없는 색 → Common"은 의도된 완화 규칙이다).

**왜 Common으로 합치면 안 되는지 (2026-09-20 실측, 구 규칙 기준):**

| 인원 | Common 묶음 | 그 묶음의 최대 연결 성분 | 최소 전환 |
|---|---|---|---|
| 4인 | 30문 (러너 색만) | 14칸 | 12 |
| 3인 | 59문 | 13칸 | **7** |
| 2인 | **89문 = 문의 절반** | **24칸** | **5** |

2인이면 패드 하나로 문 절반이 동시에 열려 러너가 거의 걸어서 골인한다.
**벽 규칙에서는 인원이 줄수록 격자가 촘촘해진다** — seed 7036 실측(러너 선택까지 28가지 전수):

| 인원 | 최소 전환 | 도달 불가 |
|---|---|---|
| 4인 | **12~13** | 0건 |
| 3인 | 13~14 | 0건 |
| 2인 | 14~15 | 0건 |
| 솔로 | 27 (흑백만) | 0건 · 흑∪백 최단 41칸 |

**배치를 다시 뽑을 필요가 없다** — 규칙이 바뀌어도 seed 7036이 전 인원에서 하한을 통과한다.
연결성은 §1.6의 **흑 ∪ 백 조건**이 전 인원에서 보장한다(흑·백은 순열·벽의 대상이 아니다).

> **⚠️ 미로 지표가 전부 무의미해졌다.** 막다른 길·루프·"정답 경로 53칸" 같은 구 기준은
> 벽이 사라지면서 같이 사라진다. 난이도를 만드는 축은 이제 **지형이 아니라 색 배치**다(§3.3).

### 1.5 패드 — 2층을 9구간으로 나누고 구간마다 전 색을 둔다 (2026-09-20 신규)

**구 설계는 "맵당 색당 1개 = 6개를 2층 곳곳에 흩어 배치"였다.** 그러면 안내자가 패드를 찾아
뛰어다니는 동안 러너가 문 앞에 서 있는다. **전환이 12회 이상인 새 구조에서는 치명적이다.**

**2층을 3×3 = 9구간(각 40×40m)으로 나누고, 구간마다 전 색 패드를 한 묶음씩 둔다.**
안내자는 **러너를 따라 위에서 이동하며 그 자리에서 누른다.**

```
 z
120 +---------+---------+---------+
    |    7    |    8    |    9    |   구간 중심 = 패드 클러스터 위치
    |(20,100) |(60,100) |(100,100)|
 80 +---------+---------+---------+   넘버패드 배치와 같다
    |    4    |    5    |    6    |   (1 = 좌하단 = 시작칸 쪽,
    | (20,60) | (60,60) |(100,60) |    9 = 우상단 = Goal 쪽)
 40 +---------+---------+---------+
    |    1    |    2    |    3    |
    | (20,20) | (60,20) |(100,20) |
  0 +---------+---------+---------+
    0        40        80       120  x
```

| 항목 | 결정 |
|---|---|
| 구간 | **3×3 = 9** · 각 40×40m |
| 구간당 패드 | **6개 authored** = 고유 4색 + 흑 + 백 |
| 배치 | 구간 중심에 **반경 4m 링**으로 6개. 안내자가 가운데 서서 밟는다 (간격 ≈ 4.2m — `OnTriggerEnter` 오발 방지) |
| 총 패드 | **54개** (9 × 6) |
| 패드 권한 | 고유색 패드 = **그 색 플레이어만** / 흑·백 = 누구나 |
| 벽이 된 색 패드 | **숨긴다** — 없는 색도 **러너 색도** 열 대상이 없다(§1.4-1) |
| Common 패드 | **없다.** T5에서 Common은 존재하지 않는다(§1.4-1) |
| 구간 이동 | 구간 중심 간 40m ≈ **4초** — 전환 1회당 예산과 맞물린다 |

**인원별로 실제로 보이는 패드:**

| 인원 | 구간에 보이는 패드 |
|---|---|
| 4인 | 고유 3 + 흑 + 백 = **5** |
| 3인 | 고유 2 + 흑 + 백 = **4** |
| 2인 | 고유 1 + 흑 + 백 = **3** |
| 솔로 | **전부 숨김**(§1.6) |

> 씬에는 **6색 54개를 그대로 authored**해 두고 숨기는 쪽으로 처리한다 — 인원·러너가 판마다 바뀌므로
> 배치는 고정이어야 한다(`ColorGateController`가 매 판 `IsSlotAbsent()`로 끈다).

> **⚠️ 이 변경이 협동을 죽이지 않는 이유.** 고유색 패드는 **그 색 플레이어만 밟을 수 있다.**
> 구간마다 전 색이 있어도 **한 명이 다 처리할 수 없고**, 안내자 전원이 러너를 따라 움직여야 한다.
> 역할이 **위치가 아니라 색으로** 갈린다. 대신 생성기가 **의도 경로에 고유 4색을 각각 2회 이상**
> 넣어야 한다(§3.3-7) — 안 그러면 한 번도 안 누르는 안내자가 생긴다.

> **규칙 변경(2026-09-20):** 구 규칙 **"2층 Common 패드는 1개만"** 은 **통째로 사라진다** —
> §1.4-1로 T5에 Common 자체가 없어졌기 때문이다. `SceneReadyAndColorMapping.md` §B.7 갱신 완료(§7).

### 1.6 솔로 — 흑·백을 동시에 열어 고정 미로로 만든다 (2026-09-20 확정)

| 항목 | 솔로 규칙 |
|---|---|
| 고유 4색 문 | **전부 벽** — 열어줄 사람이 없다(§1.4-1이 솔로까지 같은 규칙으로 덮는다) |
| 흑 문 + 백 문 | **동시에 Open 고정** |
| 2층 패드 | **전부 숨김** |
| 결과 | 솔로는 **"흑·백 문만 통로인 고정 미로"** 를 러너가 혼자 달리는 스테이지가 된다 |

> **구 규칙("솔로는 문 전부 Open")은 폐기한다.** 모든 경계가 문인 구조에서 전부 열면
> **벽이 하나도 없는 120×120 빈 들판**이 되어 대각선으로 18칸 달리면 끝난다.

**생성기 합격 조건은 이 한 줄로 요약된다 — "흑 ∪ 백 부분그래프가 start→goal을 연결할 것."**
그리고 **이 조건 하나가 1~4인 전부를 덮는다:**

- 솔로는 흑·백이 **동시에** 열리므로 통로 = (흑 ∪ 백).
- 다인승은 한 번에 한 색이지만 **흑↔백을 무한히 번갈아** 밟을 수 있으므로, 시간축을 펴면
  도달 가능 범위가 역시 **(흑 ∪ 백)**.
- 즉 **두 경우의 도달 가능성이 동일하다.** 어떤 인원에서도 "흑백만으로는 못 간다"가 나오지 않는다.

> **⚠️ 그래서 흑백 경로는 일부러 길게 뽑는다(§3.3-6).** 흑백만으로 항상 클리어가 되면 4인에서도
> 흑·백만 번갈아 밟는 것이 정답이 되고, 고유색을 든 안내자 셋이 논다 — T5를 갈아엎는 이유였던
> 문제가 그대로 재발한다. **솔로는 전환 비용이 0이라 길어도 그냥 달리면 되고, 다인승은 한 칸마다
> 전환 비용이 붙어 흑백 고집이 시간 초과로 벌 받는다.** 같은 지형이 인원에 따라 다른 난이도가 된다.

### 1.7 체이서 — 문을 관통한다 (2026-09-20 변경)

| 항목 | 결정 | 구안 |
|---|---|---|
| 수 | **4마리 고정** (인원 무관, 솔로도 4) | 동일 |
| 스폰 위치 | **동서남북 4점 고정**, 씬 루트 (§1.4 좌표) | 맵마다 16점 |
| 스폰 시점 | **러너가 시작 존을 벗어날 때** | 동일 |
| **문** | **관통한다.** `NavMeshObstacle` 없음 + 물리 레이어로 문과 비충돌 | 닫힌 문이 막음(carve) |
| **속도** | **4 m/s** (러너 10) — 실측 조정 | 8 m/s |
| 데미지 | **2** (러너 `maxHeart` 5 → **3방 사망**) | 동일 |
| 피격 후 | 소멸(Host Despawn) | 동일 |
| 리스폰 | **없다** | 동일 |
| 타겟 | 러너만. 러너가 죽거나 은신이면 정지 | 동일 |

**왜 관통인가 / 왜 속도를 반으로 깎는가:**

문이 안 막으면 체이서는 **직선**으로 온다. 반면 러너는 지그재그로 돌고 **문 앞에서 선다.**
경로 27칸 = 324m를 대기 포함 60초에 간다면, start(6,6)→goal(114,114) 직선거리 **153m** 기준
러너의 **실효 접근 속도는 약 2.5 m/s**다. 여기에 직선 8 m/s를 붙이면 4마리가 러너에게 눌러앉아
**즉사 스테이지**가 된다. 4 m/s에서 시작해 실측으로 잡는다.

> **이 변경의 진짜 소득은 속도가 아니다.**
> **위험한 순간이 "막다른 길"에서 "문 앞에서 기다리는 시간"으로 옮겨간다.**
> 안내자가 늦으면 러너가 맞는다 — 비대칭을 협동으로 묶는 장치가 패드 배타 규칙 하나뿐이었는데,
> **안내 속도가 곧 러너의 생존**이 되면서 훨씬 강한 장치가 생긴다.

> **⚠️ 체이서는 소모품이다.** 리스폰이 없고 때리면 소멸하므로 **4마리 = 러너를 때릴 기회 4번**이다.
> 3방에 죽으므로(5→3→1→0) 2방까지는 버틴다. 4마리가 다 소진되면 남은 시간은 무위험 구간이 되고
> 압박은 시계만 남는다 — **의도된 곡선**이다.

**덤으로 따라오는 정리:**
- 문에 `NavMeshObstacle`이 없으므로 **전환마다 carve 재계산이 사라진다.** NavMesh는 **빈 사각형
  바닥 1회 베이크**로 끝이다(구 문서의 "베이크 시 Doors 비활성화" 주의도 불필요).
- **체이서 스폰칸의 `degree ≥ 2` 조건이 사라진다** — 어느 칸에 놔도 갇히지 않는다.
- 문 닫힘 넉백은 `Player` 전용이라 체이서에는 안 걸린다(코드 확인 완료).
- **남는 작업:** 체이서 콜라이더가 문짝을 물리적으로 통과하도록 **레이어 충돌 매트릭스**(에디터, §5).

### 1.8 목표 / 실패 — T5 전용 Objective 하나

> **`SurviveTimeObjective`를 쓰면 안 된다.** 그건 **시간을 채우면 `Complete()`** 다 —
> T5는 시간 초과가 **실패**다.

> **`ReachZoneObjective`도 그대로는 안 된다.** 완료 조건이
> `_reachedPlayers.Count >= GameSession.GetActiveColors().Count` — **존 하나가 전원을 요구**한다.
> 1층 Goal과 2층 Goal로 나누면 양쪽 다 영원히 미완이다.

| | 내용 |
|---|---|
| 클리어 | **러너가 1층 Goal 존 + 안내자 전원이 2층 Goal 존** 안에 있을 때 `Complete()` |
| 실패 | **110초** 경과 → `Fail()` → `StageNetworkState` 직접 실패 통보 |
| 플레이어 사망 | **러너·안내자 구분 없이 누가 죽든 이 클래스가 직접 구독해 `Fail()`** |
| 판정 | Host 레인(`ReachZoneObjective`와 동일 이유 — Client가 `Complete()`를 부르면 §11A 위반) |
| UI | **시간만 표시.** 진행률 표기 없음 |

**제한 시간이 90 → 110이 된 이유 (2026-09-20 사용자 확정):**

전환 12회 구조의 시간 산수(경로 27칸 · 피치 12 기준):

| | 주행 | 전환 대기 | 합 |
|---|---|---|---|
| 최적 12회 · 안내자가 미리 눌러줌(2초) | 32초 | 24초 | **56초** |
| 최적 12회 · 반응 지연(4초) | 32초 | 48초 | **80초** |
| 실전 16회(= 최적 12의 현실값) · 3초 | 32초 | 48초 | **80초** |

90초면 최적 플레이 말고는 거의 다 실패한다. **시간이 늘어도 2층이 계속 할 일이 있으므로
지루해지지 않는다**는 판단으로 **110초**를 기본값으로 잡고, 실측 밴드는 **100~120**이다.
(`T5RunnerObjective.timeLimit`의 현재 코드 값이 `120f`이므로, 120으로 확정하면 코드 수정이 0건이다.)

**부활을 안 주는 이유:**

1. **실패가 곧 러너 교대 기회다.** 실패 → 리로드 → 러너 재추첨(§1.10). 부활을 넣으면 같은 사람이
   계속 러너가 된다.
2. **형태가 맞는다.** `SequenceRingObjective`·`ColorTileChallenge`가 전부 "실수 1회 = 판 다시"다.
3. **부활 지점 override 훅이 불필요해진다.** 2층 안내자 옆에 부활하면 러너가 2층에 갇히고
   §1.3 때문에 내려올 수 없어 타임아웃까지 교착이다.

### 1.9 2층 압박 — 기존 함정으로

**문제:** 안내자는 위험이 0이다. 러너가 죽으면 아무 잘못 없는 셋이 같이 실패한다.

**채택 — 기존 `SpikeTrap`/`ContactDamage`를 2층에 배치**(사용자 작업). 이미 있는 비용만 쓴다.
**구간과 구간 사이에 놓으면** "러너를 따라 구간을 옮기는 것 자체가 위험"이 되어 §1.5와 맞물린다.
2층 Goal 존은 안전구역으로 둔다.

**폐기한 안 — 2층 체이서 1마리:** ① §2가 "2층에 렌더 메시를 붙이지 말 것"으로 NavMesh 미베이크를
구조적 보장으로 삼는데 그걸 깨야 한다 ② `Stage5ChaserAI`는 러너 clientId만 타겟한다
③ 2인이면 안내자 1명이 도망치면서 패드를 다 밟아야 해 인원별 격차가 커진다.

> ⚠️ **이게 안내자 사망을 가능하게 만든다 → 그래서 T5는 "누가 죽든 즉시 실패"다**(§1.8).

### 1.10 러너 로테이션

**순수 랜덤 재추첨이면 같은 사람이 연속으로 뽑힌다**(4인이면 1/4).

| 항목 | 규칙 |
|---|---|
| 후보 | 전체 플레이어 − **이번 사이클에서 이미 러너를 한 사람** |
| 사이클 종료 | 후보가 비면(전원이 1번씩 함) 기록을 **리셋** |
| 저장 위치 | **`GameSession`** — 씬 리로드를 넘어 살아남아야 한다. `StageNetworkState`는 씬과 함께 사라진다 |
| 솔로 | 후보가 본인 1명이라 매번 본인 |

### 1.11 UI

- 2층 인원에게만 보이는 **러너 머리 위 마커**(`T5RunnerMarkerUI`) — 유지
- `ObjectiveUI` 타이머(남은 시간) — 유지
- `T5RunnerAnnounceUI` — **삭제 완료**(2026-09-19)
- `Tip.T.Stage5.1` 문구 교체는 **코드·테스트 완료 후 일괄**(§7)
- **지도(`T5MinimapUI`) — 신규 (2026-09-21 사용자 확정)**

#### 1.11-1 지도 — 생존시간 아래에 붙는 별도 패널

| 항목 | 결정 |
|---|---|
| 그리는 것 | **러너 위치 · start/goal 위치 · 체이서 위치**, 이 셋뿐 |
| 보는 사람 | **전원**(러너 + 안내자). 러너는 1층에서 높이 15 문에 시야가 막히고, 안내자도 문 윗면에 가려 사각이 생긴다 |
| 체이서 | **4마리 항상 표시.** 거리 제한 없음 — §1.7의 "문 앞에서 기다리는 시간이 위험"을 플레이어가 직접 계산할 수 있어야 한다 |
| 배경 | **10×10 격자선.** 빈 사각형 위의 점만으로는 위치를 못 읽는다. 문 색·열린 색은 **안 그린다** |
| 안내자 점 | **없다** — 서로 2층에서 보인다 |
| 좌표 | 월드 XZ **−12~108**(§1.4) → 지도 0~1. `worldMin/worldMax`는 `UI.prefab`에 직렬화돼 있으므로 좌표계가 바뀌면 **프리팹 값도 같이 고쳐야 한다**(코드 기본값만 바꿔서는 안 바뀐다). start/goal은 **씬 콜라이더에서 읽는다**(`T5RunnerDirector.RunnerStartZone` · `T5RunnerObjective.RunnerGoalZone`) — 좌표를 UI에 복사해 두지 않는다 |
| 네트워크 | **신규 NV/RPC 0.** 러너는 러너 clientId NV + CNT 위치, 체이서는 Host 스폰 + 서버 권한 NetworkTransform으로 전 머신에 이미 있다 |

> **⚠️ 지도는 `Objective_Panel`의 자식이면 안 된다.** `ObjectiveUI.BuildSlots()`가 `Start()`·`Refresh()`마다
> **자식을 전부 `Destroy`** 하고 `VerticalLayoutGroup`을 붙인다 — 자식으로 넣으면 스테이지 시작 순간 지워진다.
> 그래서 같은 캔버스의 **형제 패널 `T5Map_Panel`** 로 두고, 생존시간 슬롯은 `ObjectiveUI`가 그대로 굴린다
> (**`ObjectiveUI`는 한 줄도 고치지 않았다** — 표시 모드 SSOT를 T5 전용 분기로 늘리지 않기 위해서다).

### 1.12 리플레이성 — 맵이 1장인데 어디서 나오나

7장 추첨이 하던 "매판 다른 판" 역할을 **이미 있는 축 둘**이 대신한다.

| 축 | 내용 |
|---|---|
| **러너 색 → 벽** | 러너가 누구냐에 따라 그 색 문 **약 30개가 통째로 벽이 된다.** 지형이 그만큼 달라진다. 러너 로테이션(§1.10)과 맞물려 매판 구도가 바뀐다 |
| **시드 고유색 순열**(신규, §4.2 #2) | 문에 칠해진 고유 4색을 시드로 섞는다 → **매판 다른 사람이 열쇠를 쥔다.** 흑·백은 안 건드리므로 §1.6의 연결성 조건과 최소 전환 횟수는 **순열에 대해 불변** — 검증을 다시 돌릴 필요가 없다 |

### 1.13 폐기 이력

<details>
<summary>구안 — 벽 미로 7장 + 경로 위 문 15개 (2026-09-20 폐기)</summary>

12×12칸·통로 8·피치 10·120×120 미로 7장(벽 448개, 씬 배치 완료)을 만들고, 정답 경로 위에
문 15개를 사람이 직접 놓는 구조였다. 합격 기준은 정답 경로 52~64칸 · 막다른 길 10~14 ·
루프 10~14 · 오답 가지 최대 깊이 ≥6이었고, 7장 전부 그 기준을 통과해 씬에 올라가 있었다.

**폐기 이유:** 러너가 **문 앞까지는 혼자 갈 수 있어서** 2층의 할 일이 "가끔 문 열기"에 그쳤다.
경로 위 문을 늘려도, 벽 몇 개를 문으로 바꿔도 그 성질이 안 변한다는 것이 결론이었다
(2026-09-20 사용자 판단).

같이 폐기된 것: 맵 7장 추첨 · T5의 `StageVariantPicker` 사용 · 미로 생성기 `t5maze.py`/
`t5buildemit.py`와 산출물 `t5maze.json`/`t5build.txt` · 씬의 `T5_Mazes/Map_01`~`07` 전부 ·
"체이서 스폰칸 degree ≥ 2" 조건 · 구 솔로 규칙("문 전부 Open") · "Common 패드 1개만" 규칙 ·
통로 8 / 피치 10 / 12×12 좌표계.
</details>

<details>
<summary>구안 — 라운드 2개 (2026-09-19 폐기)</summary>

맵 7장 중 2장·러너 2명을 뽑아 **2라운드**를 돌리고, 라운드마다 암전 → 맵 교체 → 전원 텔레포트했다.
**폐기 이유는 기능이 아니라 형태다** — 저장소의 다른 스테이지 중 "한 스크립트가 라운드를 돌리는"
것이 없었다(§1.1).

같이 폐기된 것: `T5RunnerRoundDirector`의 라운드 상태기계 · `T5RoundState` NV ·
`StageVariantRoundPicker` · `Player.SetMovementLocked` · 라운드 알림 UI.
</details>

<details>
<summary>구안 — 맵 7장 흩어 배치 (2026-09-18 폐기)</summary>

맵을 x 200~590 / z ±65에 흩어 놓고 맵 사이 수백 m를 텔레포트했다. **원점 겹치기**로 교체.
</details>

---

## 2. 씬 구성

맵이 1장이 되면서 **"맵마다 7벌"이 전부 사라진다.**

| 씬 루트 / `T5_Common` | `T5_Maze` |
|---|---|
| `StageStartGate` + `ColoredStartZone` 4 · `LaunchPads` 4 · `LaunchShaft`(발사통 겸 시작 방 상부) | **`Floor1F`** ⚠️ |
| `Floor2F`(바닥 2장 + **`Lid_*` 4장**) · `Rails2F` | **`Doors`** (6색 그룹, 합계 **176**) |
| 1층 Goal 존 · 2층 Goal 존 | **`NavMeshSurface`** (Collect = Children) |
| 체이서 스폰 4점 · 러너 시작 존 이탈 트리거 · `T5_Arrival2F` | **`Pads2F`** (9구간 × 6 = **54**, 2층) |

**3차 보정 후 실측 좌표 (2026-09-21 — 씬에서 읽은 값이 아니라 씬에 쓴 값):**

| 오브젝트 | pos | scale | 비고 |
|---|---|---|---|
| `Floor2F/Floor2F_A` | (59.5, 50, 48.5) | (97, 0.5, 119) | 윗면 50.25 |
| `Floor2F/Floor2F_B` | (0, 50, 59.5) | (22, 0.5, 97) | 둘이 남기는 구멍이 정확히 `[−11,11]²` |
| `Floor2F/Lid_Blue` | (0, 50, 11) | (15.5564, 0.5, 15.5564) | rotY 45°. 이하 3장 동일 scale |
| `Floor2F/Lid_Purple` | (11, 50, 0) | 〃 | |
| `Floor2F/Lid_Green` | (−11, 50, 0) | 〃 | |
| `Floor2F/Lid_Yellow` | (0, 50, −11) | 〃 | |
| `LaunchShaft/Shaft_S · N` | (0, 32.625, ∓11) | (22, 35.25, 0.5) | y 15~50.25 |
| `LaunchShaft/Shaft_W · E` | (∓11, 32.625, 0) | (0.5, 35.25, 22) | |
| `LaunchShaft/Barrier_Diag_A · B` | (0, 25.125, 0) | (31.2, 50.25, 0.5) | rotY ±45°, 방 대각선 31.11 |
| `LaunchPads/LaunchPad_*` | (0,0.75,5)/(5,0.75,0)/(−5,0.75,0)/(0,0.75,−5) | (2.5, 0.5, 2.5) | `VerticalUp` **force 33** · `isTrigger` · **비활성 저장** |
| `Rails2F/Rail_S · N` | (48.5, 53.25, −11 / 108) | (119.5, 6, 0.5) | 바닥 위 6m |
| `Rails2F/Rail_W · E` | (−11 / 108, 53.25, 48.5) | (0.5, 6, 119.5) | |
| `StageManager5/Goal2F` | (102, 50.35, 102) | (6, 0.2, 6) | 트리거 실제 범위 y 49.45~54.45 |
| `T5_Arrival2F` | (48, 66, 48) | size (122, 30, 122) | **y 51~81**, 2층 전체 상공 |
| `Pads2F/Zone_*/Pad_*` | y **50.40** | — | 구간 중심 (8,8)…(88,88), 반경 4 링 |

**물리 실측 검증 (레이캐스트, 트리거 제외 — 2026-09-21):**
판 4장이 닫힌 상태에서 2층 샘플 **빈 곳 0개 · 바닥 높이 전 구간 50.25로 균일**(단차 없음) ·
전부 열면 구멍이 **정확히 x/z −11~11** · 한 장씩 열면 열리는 영역의 무게중심이
`Blue(0,+7.2)` `Purple(+7.2,0)` `Green(−7.2,0)` `Yellow(0,−7.2)`로 **삼각형 레인과 일치** ·
각 발판에서 위로 쏜 레이가 **자기 색 판에 정확히 막힘**(색↔레인 대응 확인).

> **⚠️ `Floor1F`는 반드시 `NavMeshSurface`와 같은 루트의 자식이어야 한다.**
> Surface가 **Children** 수집이라 씬 루트로 빼면 1층 바닥이 안 구워지고 **체이서가 한 발짝도 못 움직인다.**

> **⚠️ 2층은 NavMesh에 구워지지 않는다 — 그리고 그래야 한다.**
> `Floor2F`는 `BoxCollider`만 있고 `MeshRenderer`가 없는데 Surface가 **Render Meshes** 기준이라
> 자동으로 빠진다. **2층에 렌더 메시를 붙이지 말 것.**

**씬 작업 목록 (2026-09-20 — 1~5·8·10은 에디터 빌더로 처리 완료):**

| # | 작업 | 상태 |
|---|---|---|
| 1 | `StageManager5/T5_Mazes` 삭제 (Map_02~07 + 벽 67개) | ✅ |
| 2 | `T5_Maze` — **Map_01을 승격**해 `Floor1F`(122×122 @ 60,0,60)와 `NavMeshSurface`를 그대로 재사용 | ✅ |
| 3 | `Doors` 180개(6색 그룹) · `Pads2F` 54개(9구간 × 6, y=25.15) · 외곽 벽 4면 | ✅ |
| 4 | **`Goal` (114, 0.6, 114)** | ✅ (작업 전 실제 값은 **(115,115)** — 구 문서의 "(96,96)"은 오기였다) |
| 5 | **2층 Goal 존** `Goal2F` (114, 25.1, 114) | ✅ |
| 6 | **시작 홀 바닥** — 스폰 4자리(±5)를 덮게 | ✅ `T5_StartHall/Floor`에 **L자 2장**(x −7~−1 전체 · z −7~−1 전체). `Floor1F`(−1~121)와 겹치지 않게 잘라 z-fighting을 피했다 |
| 7 | 시작 홀 barrier · 발판 4개 · 투명 통로 | ✅ barrier 6장(외곽 4 @±6 + **중앙 대각 2장**, y 0.5~3.5 — `LaunchShaft` 밑단 y=3과 이어짐) · 발판 4개(VerticalUp **force 24**, 비활성 저장) · 통로는 `T5_Common/LaunchShaft`가 이미 있었다(±6, y 3~26) |
| 8 | 체이서 스폰 4점 + `Stage5ChaserSpawner` (씬 루트, §1.4 좌표) | ✅ |
| 9 | 러너 **시작 존 이탈 트리거** `T5_RunnerStartZone` | ✅ 원점 ±11 × 높이 10 — 시작칸 (0,0)의 내부가 1~11이라 **첫 문을 지나는 순간** 체이서가 나온다 |
| 10 | 잔재 삭제 — `BackGround/Stage5Barrier` · `T5RunnerAnnounce` · `StageFlow`의 `PhaseManager` | ✅ |
| 11 | `Goal`의 `ReachZoneObjective` → **`T5RunnerObjective`로 교체** | ✅ runnerGoal=Goal · guideGoal=Goal2F · timeLimit=110 · `StageManager.objectives`에 단독 등록 |
| 12 | **2층 도착 존** `T5_Arrival2F` | ✅ x 8~120 · y 25~27 · z −6~120 — **발사통(±6)을 비켜 간다**(아래 ⚠️) |

> **⚠️ 생성기가 내보내는 것은 내부 문 180개뿐이다 — 외곽 벽은 따로 세운다.**
> (그중 **시작 방 안쪽 4개를 씨에서 지욳으므로 실제 문은 176개**다 — §1.4.)
> `T5_Maze/Walls`에 4면(서 z 11~121 · 남 x 11~121 · 동·북 −1~121, 두께 2 · 높이 15)을 세우고
> **시작칸 (0,0)의 남·서만 뚫어** 시작 홀과 잇는다. 레이어는 기존 미로 벽과 같은 `Wall`.

> **✅ "2층 도착 존이 발사통 XZ를 비켜 가야 한다"는 제약은 사라졌다 (2026-09-21).**
> 그 제약은 도착 시 `DescentBlocker`(넉백)가 켜지면서 **올라가던 사람이 한 번 더 가속되는** 것을
> 피하려던 것이었다. §1.3에서 넉백을 버리고 바닥 판으로 바꾸면서 **가속원 자체가 없어졌다.**
> 그래서 `T5_Arrival2F`는 이제 **2층 전체 상공(y 51~81)을 덮는다** — 발사통 안이든 밖이든
> 상승 중에 반드시 잡히므로, 구 설계의 "x<8에 착지하면 도착으로 안 잡힌다"는 사각도 사라졌다.

> **⚠️ NavMeshSurface의 `layerMask`에서 `Door` 레이어를 빼야 한다 (2026-09-20 추가).**
> 문에 `NavMeshObstacle`이 없어도 **닫힌 문 메시가 그대로 구워지면** 체이서가 영영 관통하지 못한다 —
> §1.7이 통째로 무효가 된다. 제외하고 구운 결과는 **빈 사각형 하나**(삼각형 113)이고, 네 스폰에서
> goal까지 **직선 그대로 `PathComplete`** 임을 확인했다. 2층 패드는 베이크에 섞이지 않았다.

---

## 3. G단계 — 생성기

### 3.0 도구

`Tools/T5MazeGen/`(Unity 밖, 빌드 비포함). **미로 생성기는 역할이 끝났다.**

| 파일 | 처리 |
|---|---|
| **`t5doors.py`** (신규) | 180개 문의 **색 배치**를 뽑는다 → `t5doors.json` |
| **`t5doorsemit.py`** (신규) | `t5doors.json` → 에디터 빌더가 읽는 평문 (`t5buildemit.py`와 같은 형태) |
| `t5gen.py` | **지우지 않는다.** 그 안의 **`min_switches()`가 §3.3 검증기 본체**다 |
| `t5maze.py` · `t5buildemit.py` · `t5maze.json` · `t5build.txt` · `t5maps.json` · `t5layout.txt` · `t5emit.py` · `t5verify.py` | **폐기 대상** — 벽 미로 전용. 사용자 확인 후 삭제 |

### 3.1 생성 절차 (`t5doors.py` — 구현 완료 2026-09-20)

1. **의도 경로**(27 또는 29칸)와 **흑백 안전망 경로**(41~45칸)를 **변이 안 겹치게** 깐다.
2. 의도 경로를 **14~16구간**으로 자르고 색을 배정 — 연속 구간은 다른 색, **고유 4색 각각 2회 이상**.
3. 흑백 경로는 **흑↔백을 1~2변마다 뒤집는다** — 짧게 끊어야 "흑백만 쓰면 비싸다"가 성립한다.
4. 남은 변(약 110개)을 **같은 색이 이어지지 않게**(매칭) 채운다.
5. **지름길 보정** — 하한 미달이면 제일 싼 길의 색 사슬을 끊는다(§3.2).
6. **오답 가지**를 심고, 하나 심을 때마다 하한을 다시 본다 (깨지면 그 가지만 롤백).
7. `t5gen.py`의 `min_switches()`로 인원별 전수 검증(§3.3). 떨어지면 다음 시드.

> **⚠️ 경로 칸 수는 홀수만 가능하다.** 격자가 이분그래프라 (0,0)→(9,9) 경로는 언제나 홀수 칸이다.
> 문서 초안의 "26~30칸"에서 실제로 만들 수 있는 값은 **27과 29**뿐이다.

### 3.2 왜 뽑기가 아니라 보정인가 (2026-09-20 실측)

**격자 기하가 하한을 정한다.** (0,0)→(9,9)는 최소 18수이고, 한 색으로 L칸을 달릴 수 있으면
전환은 대략 18/L이다. 2칸짜리 색 사슬이 깔려 있으면 **무엇을 그리든 9~10전환짜리 길이 생기고,
시드를 몇천 개 뽑아도 12가 안 나온다.** 그래서 뽑지 말고 **고친다** — 제일 싼 길을 복원해
그 길이 쓰는 색 사슬을 하나씩 끊는다(`repair()`).

끊는 방법은 변의 종류마다 다르다:

| 변 | 끊는 법 |
|---|---|
| 일반 변 | 이웃이 안 쓰는 색으로 바꾼다 |
| **흑백 경로 변** | **흑↔백으로 뒤집는다** — 색은 바뀌어도 흑 ∪ 백 집합은 그대로라 솔로 연결성(§1.6)이 안 깨진다 |
| 의도 경로 변 | 건드리지 않는다 (구간 구조가 곧 설계다) |

> **⚠️ 보정이 올릴 수 있는 천장은 구간 수 S다.** 의도 경로 자체가 언제나 S짜리 길이라
> 최소 전환은 S를 넘을 수 없다. 그래서 하한 12를 얻으려면 구간을 **14~16**으로 잡는다.

**한 번 열면 달리는 거리:** 경로 29칸 / 16구간이면 한 번 열릴 때 약 1.8칸(22m, 2.2초)이다.

### 3.3 합격 기준 — 전부 자동 검증 (구현값)

| # | 기준 | 값 | seed 7036 실측(**새 벽 규칙** · 러너까지 28가지 전수) |
|---|---|---|---|
| 1 | **최소 색 전환 (4인 — 러너 색 1개가 벽)** | **12 이상** (상한 없음) | **12~13** |
| 2 | 최소 색 전환 (3인 — 벽 2색) | 10 이상 | 13~14 |
| 3 | 최소 색 전환 (2인 — 벽 3색) | 8 이상 | 14~15 |
| 4 | **솔로 = 흑 ∪ 백 연결성** | 도달 가능 + 최단 **25~45칸** | 41칸 = **49초 주행** |
| 5 | **흑백만 고집할 때의 전환** | **20 이상** | **27** |
| 6 | 고유 4색이 의도 경로에 나오는 횟수 | 각각 2회 이상 | 충족 |
| 7 | **오답 가지** | **4칸 이상 × 6개** | 6개 |
| 8 | 색당 문 개수 | 18~45 | 25~37 |

**4·5번이 §1.6의 두 얼굴이다.** 4번은 "흑백만으로도 갈 수는 있다"(솔로 성립 + 다인승 안전망),
5번은 "그런데 비싸다"(4인 12전환 vs 흑백만 27전환)를 보장한다. 둘이 같이 있어야
"인원이 적으면 느리지만 막히지는 않는다"가 성립한다.

**검증기:** `t5gen.py`의 `min_switches()` — 상태 = **(칸, 열린 색)**, 색 전환 1회 = 비용 1,
이동 = 비용 0. **벽은 open_edges에서 빼서 표현한다**(`passable()`) — 검증기를 새로 만들지 않는다.

> **⚠️ 벽 규칙 덕분에 인원이 줄수록 전환이 늘어난다.** 구 규칙(없는 색 → Common)에서는
> 2인이 5전환이었는데(§1.4-1), 벽 규칙에서는 **14~15전환**이다. 대신 2인은 안내자가 1명이라
> 패드 왕복도 길다 — **시간 압박이 인원에 따라 가파르게 오른다. V단계 최우선 확인 대상.**

> **⚠️ 검증기는 러너가 누구인지까지 전수로 돌린다(28가지).** 배치가 대칭이 아니라 같은 인원이어도
> **러너가 누구냐에 따라 답이 다르다**(4인 12 또는 13). 구 `partitions()`는 러너를 `UNIQUE[0]`으로
> 고정하고 있었다 — 2026-09-20에 고쳤다.

> **⚠️ 12는 "최적값"이다.** 안내자가 순서를 틀리고 러너가 오답 가지에 들어가므로
> **실전 체감은 15~18회**다. 110초는 그 값을 기준으로 잡은 것이다(§1.8).

> **⚠️ 시드 색 순열은 전환 수를 바꾸지 않는다.** 고유 4색끼리의 교환은 라벨 교체라
> 결과가 불변이다 — 순열마다 재검증할 필요가 없다(§1.12).

**오답 가지 깊이가 6 → 4로 내려간 이유 (2026-09-20 실측).**
가지는 **지름길이 되면 안 되는데 복도는 양방향이다** — 목표 반대로 뻗어도 러너가 먼 끝에서
뿌리 쪽으로 타면 그게 곧 전진이다(실제로 전환 5회짜리 고속도로가 나왔다). 그래서 뿌리 반경 3 안에
**똬리를 틀게** 묶었고, 그 제약에서 6칸짜리는 1231번 시도에 23번만 자라고 그중 6번만 하한을
안 깨뜨렸다. **4칸이면 왕복 8칸 = 96m ≈ 10초 손해**라 벌로는 충분하다.

### 3.4 씬 빌드

`t5doorsemit.py`가 `t5doors.json` → **`t5doors.txt`**(235줄)로 내린다. 한 줄 = 한 오브젝트:

```
GRID 10 12.0 120.0 10.0 2.0 15.0          n · 피치 · 범위 · 문폭 · 두께 · 높이
DOOR DoorV_0_0 12.0 6.0 2.0 10.0 Black    이름 · cx · cz · sx · sz · 색
PAD  Pad_Z9_White 102.0 96.536 White 9    이름 · cx · cz · 색 · 구간번호
```

| 것 | 값 |
|---|---|
| 문 이름 | `DoorV_<i>_<j>` = 칸 (i,j)-(i+1,j) 사이 / `DoorH_<i>_<j>` = 칸 (i,j)-(i,j+1) 사이 |
| 문 오브젝트 | 폭 10 · 높이 15 · 두께 2 · y = 7.5 · `Rigidbody`(`DoorController` 요구) |
| 부모 | `Doors/Blue` … `Doors/White` (6색 그룹 → `ColorGateController.doorGroups`) |
| `NavMeshObstacle` | **붙이지 않는다**(§1.7) |
| 패드 | `Pads2F/Zone_1`~`Zone_9` 아래 6개씩, y는 2층 윗면(25.0) |

`t5doorsemit.py`는 격자 미리보기도 같이 찍는다 — 칸 사이 글자가 그 경계의 문 색이고
(B/P/G/Y = 고유, K = 흑, W = 백), **대문자가 의도 경로 위의 문**, `o`가 의도 경로가 지나는 칸이다.

## 4. C단계 — 코드

### 4.1 이미 되어 있는 것 (그대로 쓰는 것)

- `T5RunnerDirector` — 단계 스위치(발판 끄기 · barrier 해제 · 체이서 스폰 · 게이트 해제)
- `T5RunnerObjective` — 두 Goal 존 + 타이머 + **누가 죽든 `Fail()`**(`Tick()`의 `AnyPlayerDead()`)
- `StageNetworkState` — 러너 clientId NV 1개 + `OpenGateColor` 슬롯
- `ColorGateController` / `ColorGatePad` / `DoorController` — 배타 게이트·색 권한·SlideUp·닫힘 넉백
- `SessionColorSlotMap` — 없는 색 + 러너 색을 **벽**으로 판정(`IsSlotAbsent`) · 시드 순열(`SetDesignPermutation`)
- `Stage5ChaserAI` / `Stage5ChaserSpawner` / `Stage5ChaserHitbox` — 4마리 1회 스폰, 리스폰 없음

### 4.2 새로 해야 할 것

| # | 대상 | 내용 | 상태 |
|---|---|---|---|
| 1 | `T5RunnerDirector` | **`mapPicker` 제거** — 맵이 1장이라 `StageVariantPicker`를 안 쓴다. 맵 활성화 분기도 단순화 | 완료 |
| 2 | **`SessionColorSlotMap`** | **`SetDesignPermutation(int seed)` 신규** — `DesignSlots` 4색을 시드로 섞어 `Resolve()` 앞단에 한 겹 더 얹는다(`Permute()`). Black/White는 불변. 값이 바뀔 때만 `Version++`, `Rebuild`/`Clear`에서 초기화 | 완료 |
| 3 | `T5RunnerDirector` | `OnPlayersReady`에서 **#2 → `SetRunnerExclusion()`** 순서로 적용 | 완료 |
| 4 | `T5RunnerObjective` | `timeLimit` **110** (현재 `120f`) + 클래스 주석의 "timeLimit = 120" 갱신 | 완료 |
| 5 | **`ColorGateController` · `ColorGatePad`** | **벽 규칙(§1.4-1)** — **없는 색도 러너 색도** 그룹 영구 폐쇄 + 패드 숨김. ⚠️ `Resolve()`는 다른 스테이지의 `PressurePad`도 쓰므로 전역 의미는 안 건드렸다 — 판정용 질문 `SessionColorSlotMap.IsSlotAbsent()`를 새로 두고 T5만 그것을 본다. **솔로 흑·백 고정 개방**(§1.6)도 같이 들어갔다 | 완료 |
| 6 | `GameSession` | **러너 로테이션 기록**(§1.10) — 이번 사이클에서 러너를 한 clientId 집합, 씬 리로드를 넘어 유지 | 완료 |
| 7 | `T5RunnerDirector` | `DrawRunner()`가 로테이션 집합을 제외하고 뽑기 (현재는 시드 셔플만) | 완료 |
| 8 | `ColorGateController` · `ColorGatePad` | 클래스 주석이 구 절 번호·"맵당 6개"·"맵 루트마다 1개"를 가리킨다 → 새 구조로 갱신 | 완료 |

> **⚠️ #6·#7이 빠져 있으면 §1.8의 "부활을 안 주는" 명분이 성립하지 않는다.**
> 지금 `DrawRunner()`는 순수 시드 셔플이라 **연속 2판에서 같은 사람이 러너가 될 수 있다.**

| 9 | **에디터 빌더** | `t5doors.txt`를 읽어 문 180 + 패드 54를 씬에 만드는 1회용 스크립트(`execute_code`). 좌표·이름·색은 §3.4 형식 그대로 | 완료 |
| 10 | **`T5RunnerDirector`** | **`descentBlocker` 필드 삭제 → `FloorPlate[] floorPlates` 신설**(색 + `Lid_*`). `Start()`에서 전부 닫힘 | 완료(2026-09-21) |
| 11 | `T5RunnerDirector` | `PickRunnerAndColors()`에서 **`OpenGuidePlates()`** — 명단에 있고 러너가 아닌 색만 연다. 명단에 없는 색은 순회에 안 걸려 자동으로 닫힌 채 남는다 | 완료(2026-09-21) |
| 12 | `T5RunnerDirector` | `TickArrival()`을 **안내자별 래치**(`_arrivedGuides`)로. 한 명 잡힐 때마다 그 색 판 닫기 + `NetLog`, 전원이면 `ReleaseStart("arrival")` | 완료(2026-09-21) |
| 13 | `T5RunnerDirector` | `ReleaseStart()` 순서를 **남은 판 닫기 → 발판 끄기 → barrier 해제**로 고정. `arrivalFallbackSeconds` **5 → 8** | 완료(2026-09-21) |

**코드가 아닌 것 (에디터 — §5):** 체이서 속도 4 · 데미지 2 · 문 레이어 비충돌 · NavMesh 1회 베이크.

### 4.3 네트워크 권한

**러너 clientId만 NV 1개.** 맵 인덱스는 **사라졌고**, 색 순열은 **시드에서 나오므로 NV에 싣지 않는다**
(전 머신이 같은 답을 낸다 — `SceneReadyAndColorMapping.md` §C의 "시드에서 나오는 답은 NV에 싣지 않는다").

| 것 | 어디서 | 왜 |
|---|---|---|
| 고유색 순열 | 시드 (NV 없음) | 전 머신 동일 계산 |
| 러너 clientId | **NV 1개** | Host 전권인 체이서가 읽는다 |
| 열린 색 | **NV** (`StageNetworkState.OpenGateColor`) | Host만 쓰고 전 머신이 수렴 |
| 발판 끄기·barrier 해제 | 각 머신 로컬 (NV/러너에서 파생) | 시각 연출 + 콜라이더 토글 |
| 체이서 스폰 | Host | 기존 그대로 |
| 문 개폐 | Host (`ColorGatePad` → `ColorGateController`) | 기존 그대로 |

---

## 5. 배선 체크리스트 (에디터)

씬 재제작(§2)과 문·패드 생성(§3.4)은 끝났다. **✅ = 2026-09-20 완료, ⚠️ = 남은 것.**

1. ✅ **`T5RunnerDirector`** (2026-09-21 재배선) — `launchPads` **4개**(색별 `LaunchPad_*`) ·
   `floorPlates` **4개**(색별 `Lid_*`) · `barriers` **2개**(`Barrier_Diag_A/B`) ·
   `arrivalZone2F`(`T5_Arrival2F`) · `startZone`(`T5_RunnerStartZone`) · `chaserSpawner` ·
   `arrivalFallbackSeconds` **8**.
   ⚠️ **발판과 판은 색이 서로 맞아야 한다** — Blue 발판에서 올라간 사람은 Blue 판 밑으로 나온다.
   `mapPicker`·`descentBlocker`는 **사라졌다**(§4.2-1 · §1.3).
   `ColorGateController`는 배선하지 않는다 — 디렉터가 자동으로 찾는다
2. ✅ **`LaunchPads` 4개에 `ContactKnockback` 부착** — `VerticalUp` **force 33**(min=max, §1.2) ·
   `knockbackInterval` 0.25 · 콜라이더 `isTrigger = true` · 오브젝트는 **비활성 저장**
   (카운트다운 완료 때 디렉터가 켠다). 트리거로 두는 이유는 켜지는 순간 밑에서 솟아 플레이어를
   밀어 올리는 일이 없게 하려는 것이고, `OnTriggerStay`가 있어 **이미 밟고 선 사람에게도 발동한다**
3. ✅ **`T5RunnerObjective`** — 1층/2층 Goal 존 연결, `timeLimit` **110**,
   `StageManager.objectives`에 단독 등록. `StageNetworkState.disableRevive`는 이미 체크돼 있었다(§7.1)
4. `StageStartGate` — `countdownDuration` 3, `ColoredStartZone` 4개 *(기존 배선 유지)*
5. ✅ **`ColorGateController`** — `T5_Maze`에 부착, `doorGroups` 6색 루트 연결, `pads`는 비움(자동 수집 54개 확인)
6. ✅ 문 **176개**: `openMode = SlideUp` · **`openAmount 9` · `duration 0.35`** · `latchOnOpen = false` ·
   `requiredPads` 비어 있음 · **`NavMeshObstacle` 없음** · 레이어 `Door`
   (구 주석의 "2층 바닥 밑면 24.5라 SlideUp 최대 9.5"는 **2층이 50으로 올라가면서 무의미해졌다** —
   이제 천장 제약이 없고, 9는 문 높이 15에서 9m가 열린다는 뜻일 뿐이다)
7. ✅ 패드 54개: `designColor` 지정, `Pads2F/Zone_1`~`Zone_9` 아래 6개씩, y=25.15 · 트리거 두께 0.3
8. ✅ `Chaser.prefab`: `moveSpeed` 8→**4** · `NavMeshAgent.speed` 8→**4** · `Stage5ChaserHitbox.damage` 1→**2** ·
   `postHitStopDuration` 2→**0.5**
9. ✅ **물리 레이어** — `Door` 레이어 신설(11) + **Enemy × Door 충돌 해제**(§1.7 관통).
   `Player × Door`는 유지 확인. `ProjectSettings/DynamicsManager.asset`에 저장됨
10. ✅ **NavMesh 1회 베이크** — `T5_Maze`의 `NavMeshSurface`(→ `Assets/Scenes/T.Stage5/NavMesh-T5_Maze.asset`).
    **`layerMask`에서 `Door` 제외**가 전제다(§2의 ⚠️). 문을 끌 필요는 없다
11. ~~Common 전용 무채색 머티리얼~~ — **T5에서는 불필요해졌다**(§1.4-1: Common이 없다).
    T1/T4용으로는 `SceneReadyAndColorMapping.md` §B.5에 그대로 남아 있다
12. ✅ **`T5Map_Panel`**(§1.11-1) — `UI.prefab` 캔버스 아래 빈 패널 + `T5MinimapUI` 하나.
    배경·격자선·점은 코드가 만든다. `Objective_Panel`(상단 중앙, `(0,-70)`, 370×120) **아래**로 위치·크기 조정.
    프리팹에서는 **비활성**으로 저장하고 `T.Stage5` 씬의 UI 인스턴스에서만 활성 —
    `BossHealBar_Panel`이 `T.Boss`에서만 켜지는 것과 같은 방식이다.

> **배선은 전부 끝났다(2026-09-21).** 지도 패널은 씬 인스턴스에서 활성, `worldMin/Max`는 −12/108로 정정(start → 지도 (0.10, 0.10), goal → (0.95, 0.95) 확인). 남은 것은 §6 V단계 실측과 2층 압박용 함정 배치(§1.9)다.

---

## 6. V단계 (사용자 — 빌드 2개 환경)

**시작 시퀀스**
- 커튼이 걷힐 때 **문 색이 이미 확정돼 있는가** (게이트 `t5.map`)
- 3초 뒤 안내자만 발사되고 **러너는 그 자리에 남는가**
- 발사가 2층에 **닿는가**(force 33 / 정점 56.5) — 모자라면 재발사로 복구되는가
- **§1.3 판 검증 — 이번 V단계 최우선.**
  · 안내자가 **자기 판 위에 착지하는가**(상승 중 51 통과 → 발밑에서 닫힘). 판이 몸 안에서 생성되지 않는가
  · **러너 색·없는 색 레인이 끝까지 닫혀 있는가**(3인·2인·솔로에서 각각)
  · 한 명이 일부러 발판을 안 밟았을 때 **나머지 셋은 정상으로 끝나는가**, 8초 뒤 남은 판이 닫히는가
  · 착지 후 판에서 2층 바닥으로 **걸어 나갈 때 걸리는 데가 없는가**(실측상 단차 0)
- 전원 도착 시점에 **남은 판 → 발판 → barrier 순서**로 풀리는가, 러너가 그제야 문 쪽으로 나가는가
- 러너가 시작 존을 벗어날 때 **체이서 4마리**가 나오는가 (솔로 포함)

**핵심 — 새 구조에서 처음 확인하는 것**
- **문 176개 동시 존재의 부하** — 전환 1회에 약 60개가 움직인다(§1.4). 프레임 드랍·네트워크 튐
- **체이서가 문을 실제로 통과하는가** (레이어 매트릭스) / NavMesh를 벗어나 끼지 않는가
- **4 m/s가 체감상 맞는가** — 문 대기 중 접근 압박이 있는가, 열린 직후 따돌릴 수 있는가
- **최소 전환(12 이상)이 실전에서 몇 회로 늘어나는가** — 최소 12 기준 15~18 예상. 110초의 근거
- **안내자가 구간을 따라 이동하는 흐름이 성립하는가** — 40m ≈ 4초가 병목인가
- **2인**: 안내자 1명이 3종류(고유 1·흑·백)를 혼자 밟는 체감. **최소 전환이 14~15로 늘어나므로
  110초가 모자라는지**가 핵심 — 여기가 이번 규칙 변경의 가장 큰 리스크다
- **솔로**: 흑백 고정 미로로 클리어 가능한가, 몇 초 걸리는가(예상 42~48초 + 회전 감속)
- 문턱에서 전환을 맞으면 러너가 튕기는가 — **사양이지만 빈도가 과한지**(§1.4 ⚠️)

**클리어·실패**
- 러너 1층 Goal + 안내자 전원 2층 Goal → 클리어
- 110초 → 실패 → 리로드 시 **러너·색 순열 재추첨**
- 러너/안내자 사망 → **즉시 실패** → 리로드
- **러너 로테이션**(§1.10) — 연속 2판에서 같은 사람이 러너가 되지 않는가

---

## 7. 문서 갱신 (2026-09-20)

- `SceneReadyAndColorMapping.md`: T5에서 Common이 사라진 것 반영(§B.1·§B.7·§B.8) ·
  §C.1의 `StageVariantRoundPicker` 행 · §D.2 T5 배선 · §E "T5 라운드 전환" — **✅ 완료**
- `CoopStageAudit.T.md`: T5 "보류·재설계 금지" 해제 → 이 문서 링크 — **✅ 완료**
- `TStageNetworkBoard.md` §3.2: 러너 한정 타겟 · 4마리 · 리스폰 없음 · **문 관통/속도 4** — **✅ 완료**
- `NetworkDesign.md` §11.9(라운드 중 텔레포트): T5가 더는 쓰지 않음을 표시.
  **삭제하지 않았다** — 텔레포트 자체의 SSOT다 — **✅ 완료**
- `Tools/T5MazeGen/README.md`: 벽 규칙 + 폐기 파일 삭제 완료 — **✅ 완료**
- `Tip.T.Stage5.1` 문구 + 13개 로케일: **V단계 후 일괄 교체 — 미완**

---

## 8. 발 헛디디기 쉬운 곳

- **`TipUI`는 String Table을 먼저 읽는다.** 코드 폴백만 고치면 화면은 하나도 안 바뀐다.
  실제로 이 함정에 빠져 "갱신했다"고 잘못 보고한 적이 있다.
- **`ColorGateController.SnapAllClosed()` → `ApplySlotMapping()` 순서.** 스냅이 "적용 완료" 표식을
  세우므로 재매핑이 뒤에 와야 한다.
- **"패드 위에 서 있는데 내 문이 안 열린다" = 사양이다**(§1.4). `OnTriggerStay` 재발동을 넣지 말 것 —
  두 안내자가 각자 패드에 서 있으면 프레임마다 색이 뒤집혀 문이 떨리고 NV가 초당 수십 회 나간다.
  **구간마다 패드가 6개씩 모여 있는 새 배치에서는 이 위험이 더 커진다** — 링 간격 4m를 지킬 것.
- **`ContactKnockback`은 대상을 보지 않는다.** 러너 제외는 **발판을 끄는 것**이 유일한 수단이다(§1.2).
  ("속도 방향도 안 본다 → 이중 가속" 쪽은 §1.3에서 넉백을 버리면서 **더는 문제가 아니다.**)
- **`Lid_*`는 켜짐이 "닫힘"이다.** `SetActive(true)` = 바닥 있음 = 구멍 없음. 활성 상태와 말뜻이
  반대라 조건문을 뒤집기 쉽다.
- **판을 "열어두고 도착 때 닫는" 방식으로 되돌리지 말 것.** 러너와 없는 색 레인은 **아무도
  올라오지 않아 트리거가 영영 안 걸린다** — 그 구멍으로 안내자가 1층에 떨어진다(§1.3).
- **뚜껑을 한 장으로 합치지 말 것.** 한 명이 발사에 실패하면 나머지가 무한히 튀어오르고,
  그 상태에서는 닫아도(갇힘) 안 닫아도(영영 미완) 교착이다(§1.3).
- **2층 도착 판정선은 51이지 50.25가 아니다.** 바닥 높이로 잡으면 판이 발 안쪽에서 생성되거나
  이미 떨어진 뒤에 닫힌다(§1.3).
- **`arrivalFallbackSeconds`는 기본값을 코드에서 바꿔도 씬에 이미 직렬화된 값이 안 바뀐다.**
  5 → 8 변경은 씬의 컴포넌트 값을 직접 고쳐야 반영된다(2026-09-21에 실제로 그렇게 했다).
- **발사통 하단(y 0~15)에는 벽이 없다 — 그래야 러너가 문으로 나간다.** 그 구간은 방 자체의
  문·외벽이 막는다. 발사통을 0부터 세우면 **러너가 시작 방에서 영원히 못 나온다.**
- **`DoorController`의 닫힘 넉백은 `Player`에만 걸린다** — 체이서는 안 밀린다(코드 확인 완료).
  **러너는 밀린다**(§1.4 ⚠️).
- **`ReachZoneObjective`는 존 하나가 전원을 요구한다**(§1.8). 그대로 두 개 쓰면 둘 다 미완이다.
- **`SurviveTimeObjective`는 시간을 채우면 클리어다**(§1.8). T5와 승패가 반대다.
- **`Floor1F`를 `NavMeshSurface` 바깥으로 빼면 체이서가 안 움직인다**(§2).
- **좌표계가 또 바뀌었다**: −50~+50 → 0~100 → 0~120(피치 10 · 12×12) → 0~120(피치 12 · 10×10)
  → **−12~108(피치 12 · 10×10, 2026-09-21)**.
  **칸 중심이 6·18…114에서 −6·6·18…102로**, **goal이 (114,114)에서 (102,102)로**,
  **start가 (6,6) 한 칸에서 원점 중심 2×2 방으로** 바뀌었다 —
  구 스크립트·구 문서 값을 그대로 쓰면 전부 어긋난다.
- **시작 방의 출구는 4개가 아니라 6개다.** 문 4개를 지우면서 격자 교차점 `(12,0)`·`(0,12)`의
  2×2 빈칸이 방과 이어졌고, 거기서 `DoorH_2_0`·`DoorV_0_2`로 한 칸 더 나갈 수 있다.
  생성기 검증(최소 전환 12)은 이 삭제를 모르는 값이지만, **문이 없어진 쪽은 길이 쉬워지는 방향**이라
  재검증은 하지 않기로 했다(2026-09-21 사용자 판단).
- **격자 기하가 전환 수의 하한을 정한다**(§3.2). 최소 18수 ÷ 색 사슬 길이 ≈ 전환 수라,
  2칸 사슬이 깔려 있으면 시드를 몇천 개 뽑아도 12가 안 나온다. 보정(`repair`)이 그래서 있다.
- **최소 전환은 구간 수 S를 못 넘는다**(§3.2). 의도 경로가 언제나 S짜리 길이기 때문이다.
- **오답 가지 복도는 양방향이다**(§3.3). 목표 반대로 뻗어도 먼 끝에서 뿌리로 타면 지름길이 된다 —
  뿌리 반경 안에 똬리를 틀게 묶어야 한다.
- **"없는 색 → Common"을 문 180개에 그대로 적용하면 2인에서 문의 절반이 한 번에 열린다**(§1.4-1).
  구 15문짜리 설계에서 넘어온 규칙이라 규모가 안 맞는다. **러너 색도 같은 이유로 벽이다.**
- **`SessionColorSlotMap.Resolve()`의 의미를 T5 사정으로 바꾸지 말 것.** T1/T4의 `PressurePad`가
  "없는 색 → Common(누구나)"에 기대고 있다. T5는 `IsSlotAbsent()`라는 **별도 질문**으로 가른다.
- **닫힌 문 메시는 NavMesh에 구워진다.** `NavMeshObstacle`이 없다고 베이크에서 빠지는 게 아니다 —
  `NavMeshSurface.layerMask`에서 `Door`를 빼지 않으면 체이서가 문을 관통하지 못한다(§2 ⚠️).
- **생성기는 내부 문 180개만 내보낸다.** 외곽 벽은 씬에서 따로 세워야 한다(§2 ⚠️).
- **`arrivalZone2F`는 AABB 판정이다.** 2층 전체를 덮으면 발사통을 지나던 안내자가 도착으로 잡혀
  `DescentBlocker`에 **이중 가속**돼 난간을 넘는다(§2 ⚠️). 발사통 XZ를 비켜 갈 것.
- **`LaunchShaft` 윗단이 y=26이다.** 2층 바닥(25)만 보고 발사 힘을 정하면 통을 못 빠져나온다(§1.2).
- **에디트 모드에서 새로 만들거나 옮긴 콜라이더는 `Physics.SyncTransforms()` 전에는 레이캐스트·
  `bounds`에 안 잡힌다.** 검증 스크립트가 "바닥 없음"이라고 거짓 보고한 적이 있다.
- **검증기의 `partitions()`는 러너 색까지 전수로 돌려야 한다.** 배치가 대칭이 아니라 러너가 누구냐에
  따라 최소 전환이 달라진다(§3.3 ⚠️).
- **경로 칸 수는 홀수만 가능하다**(§3.1). 짝수를 넣으면 생성기가 조용히 실패한다.
- ~~`Goal`이 (96,0.6,96)에 남아 있다~~ — **문서가 틀렸었다.** 실제 값은 (115,115)였고,
  지금은 (114, 0.6, 114)로 맞췄다(§2-4). 씬 값을 문서에 옮겨 적을 때는 MCP로 실측할 것.
- **시작 홀 스폰 두 자리에 바닥이 없다**(§2-6). `Ground`가 z=−68로 이동했고 `Floor1F`는 −1부터다.

---

## 9. 다음 단계

1. ~~D — 설계 확정~~ → **완료.** §1 전체 (+ §1.4-1로 러너 색까지 벽으로 통일)
2. ~~G — 생성기~~ → **완료.** `t5doors.py` + `t5doorsemit.py`, 검증기는 `min_switches()` 재사용.
   **확정 배치: seed 7036** — 벽 규칙이 바뀐 뒤에도 전 인원 통과(§3.3)
3. ~~S — 씬 재제작~~ → **완료.** 문 180 · 패드 54 · 외곽 벽 · Goal/Goal2F · 체이서 스폰 4점 ·
   Door 레이어 + 충돌 매트릭스 · NavMesh 베이크. **남은 것은 시작 홀(§2-6·7·9)뿐이다**
4. ~~C — 코드~~ → **완료.** §4.2 전건
5. ~~시작 홀 + 배선~~ → **완료.** 이후 **3차 보정(2026-09-21)** 으로 전면 교체됐다 —
   시작 홀이 격자 안 2×2 방이 되고, 2층이 50으로 올라가고, 발판 force 24 → **33**,
   하향 차단이 넉백 → **레인별 바닥 판**, `arrivalZone2F`가 2층 전체로 확대(§2 실측표).
6. **V — §6 ← 지금 여기.** 우선순위: ① **레인별 판이 제대로 닫히는가**(§1.3)
   ② 발사가 실제로 2층에 닿는가(force 33) ③ 문 176개 부하
   ③ 문 관통 ④ 4 m/s 체감 ⑤ 실전 전환 횟수 ⑥ **2인 시간 압박**(최소 전환 14~15)
7. **2층 압박 함정 배치**(§1.9 — 구간 사이에 `SpikeTrap`/`ContactDamage`)
8. Tip·대사 문구 일괄 교체
